overwrite: true
schema: "graphql/schema/schema.graphql.ts"
documents: null
generates:
  graphql/generated/graphql.ts:
    config:
      contextType: "../types#AppContext"
    plugins:
      - "typescript"
      - "typescript-resolvers"
